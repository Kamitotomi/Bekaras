<table class="table">
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% posts.each do |post| %>
      <tr>
        
        <td><%= link_to post.body, post_path(post.id) %></td>
        <td><%= post.category == 0 ? "行動" : "言葉" %></td>
        
      </tr>
    <% end %>
  </tbody>
</table>